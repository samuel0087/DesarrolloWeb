/**
 * Ejercicio 25
 * Crear un array de ingredientes para una receta absurda.
 * Muestra la receta completa en una frase
 */

const ingredientes = ["Massa Tarta", "Palta", "Papas fritas", "Queso cheddar"];

let frase = `Receta absurda: Tarta de veggi: ${ingredientes.join(', ')}`;
console.log(frase);